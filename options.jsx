import React from "react"

function Options() {
  return (
    <div
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        height: "100vh",
        fontSize: 32,
        fontFamily: "sans-serif"
      }}>
      Options page loaded!
    </div>
  )
}

export default Options
